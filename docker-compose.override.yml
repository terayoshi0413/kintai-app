version: '3'

services:
  phpmyadmin:
    image: phpmyadmin/phpmyadmin
    container_name: kintai-app-phpmyadmin
    environment:
      PMA_HOST: mysql
      PMA_USER: sail
      PMA_PASSWORD: password
    ports:
      - 8080:80
    depends_on:
      - mysql